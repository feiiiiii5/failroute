id: failroute
name: failroute
description: Detect failure-routing anti-patterns (silent exception swallowing, silent fallback returns)
entry: failroute
language: python
types: [python]
require_serial: true
