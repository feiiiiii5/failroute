id: failroute
name: failroute
description: Detect failure-routing anti-patterns (swallowed exceptions, silent fallback values, contextlib.suppress blocks)
entry: failroute
language: python
types: [python]
require_serial: true
